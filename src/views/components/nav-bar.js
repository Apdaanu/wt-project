import React from 'react';

const AppNavBar = props => {
  return <div style={{height: 100, display: 'flex'}}>
    <div>This is nav bar, will be done by aditya</div>
    <div style={{width: 100}} />
    <div>Login</div>
  </div>;
}

export default AppNavBar;